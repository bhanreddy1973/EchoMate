graph TD
    subgraph RecapPanel["RecapPanel — top-right"]
        R1["Receives CompletedItem list from SpatialDashboard"]
        R2["Displays recently completed tasks with timestamps"]
    end

    subgraph ReadingListPanel["ReadingListPanel — bottom-right"]
        R3["Curated reading links and resources"]
    end
