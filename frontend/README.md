# ⚠️ Legacy Frontend (Prototype)

This Vite + React frontend is a **legacy prototype** and is no longer the primary UI.

## The main frontend is at `../app/` (Next.js)

To run the current UI:

```bash
cd ../app
npm install
npm run dev
```

Visit http://localhost:3000

---

This folder is kept for reference but may be removed in a future release.
